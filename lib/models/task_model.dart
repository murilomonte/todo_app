
class TaskModel {
  int id;
  String title;
  String description;
  double points;
  int status;
  TaskModel({
    required this.id,
    required this.title,
    required this.description,
    required this.points,
    required this.status,
  });
}
